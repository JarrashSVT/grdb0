
#include "graph.h"
#include <stdbool.h>
#include <stdlib.h>

void 
add(vertexid_t v_id, bool is_visited)
{
	//visited_list vlist;
	//vlist = (visited_list) malloc(sizeof(vlist));
	//vlist->v_id = v_id;
	//vlist->is_visited = is_visited;
}

bool lookup(vertexid_t v_id)
{
	return true;
}